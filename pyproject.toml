[project]
name = "podcast"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "plan10",
    "feedparser",
    "trafilatura",
    "bs4>=0.0.2",
]

[tool.uv.sources]
plan10 = { git = "https://github.com/noodlepopllc/Plan10.git" }

